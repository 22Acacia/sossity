machine:
  java:
    version: oraclejdk8

test:
  override:
   - lein test

deployment:
  demo:
    branch: master
    commands:
      - cd $HOME/$CIRCLE_PROJECT_REPONAME && lein uberjar
      - gsutil cp $HOME/$CIRCLE_PROJECT_REPONAME/target/sossity-0.1.0-SNAPSHOT-standalone.jar gs://build-artifacts-public-eu/sossity-standalone.jar
